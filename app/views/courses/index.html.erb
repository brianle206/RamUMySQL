<h1>Course Index</h1>

<%= link_to 'Create new Course', new_course_path %>

<% @course.each do |course| %>
	<h1><%= course.title %> <%= link_to "Edit", edit_course_path(course) %> <%= link_to "Destroy", "/courses/#{course.id}", :method => 'delete'%></h1>
	<p><%= course.description %></p>
	
<% end %>
