<div class='container'>
	<h1 class="text-center">RAM University Courses</h1>
</div>

	<% if user_signed_in? %>
	<div class='container'>
		<%= render 'learns/course_partial' %>
	</div>
	<% else %>
		<%= render 'learns/intro_partial' %>
	<% end %>

